#include "main.h"

/**
 * _abs - compute absolute value of n
 * @n: The input number that is an integer.
 * Return: always 0
 */
int print_sign(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	}
		return (0);
	}
}
